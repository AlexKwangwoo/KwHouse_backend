version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    # build:
    #   commands:
    #     - if [ "${AWS_BRANCH}" = "main" ]; then npm run build-main; fi
    #     - if [ "${AWS_BRANCH}" = "uat" ]; then npm run build-uat; fi
    #     - if [ "${AWS_BRANCH}" = "dev" ]; then npm run build-dev; fi
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
